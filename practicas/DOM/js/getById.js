/**
 * Solo debe de haber el mismo valor para un id
 * en un mismo documento
 */


const fomulario = document.getElementById('formulario');
console.log(fomulario);

// si se selecciona un elemento que no existe, retorna null

const noExiste = document.getElementById('no-existe'); 
console.log(noExiste);