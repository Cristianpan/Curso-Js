/**
 * Al igual que let, const tiene un alcance de bloque y a diferencia de 
 * let y var, las variables con const siempre deben de ser inicializadas. 
 * Las variables const representan constantes por lo que son variables inmutables.
 */

const pi = 3.141516

console.log(pi);